/**
 * Stub for CSS imports.
 *
 * `src/constants/theme.ts` imports `@/global.css` for NativeWind, which Metro
 * understands and Jest does not. Styling is not what these tests assert on.
 */
module.exports = {};
